# Copy to .env and fill in. Never commit the real key.
# Set this in the Render dashboard for production (Environment > Add Secret).
ANTHROPIC_API_KEY=sk-ant-...

# Optional. Render injects PORT automatically; defaults to 3000 locally.
PORT=3000
