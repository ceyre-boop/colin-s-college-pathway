# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Secrets
.env
.env.local
.env.*.local

# Applicant PII + auto-apply run artifacts (never commit — real DOB/phone/screenshots)
# NB: the `*.local` rule above does NOT match `applicant.local.json`, so list it explicitly.
applicant.local.json
apply/results/
apply/.userdata/

# Event log + derived projection. The log records values typed into forms, so it is PII —
# tamper-evidence comes from the hash chain in state/log.ts, not from git.
state/events.jsonl
state/projection.json
state/queue.json

# Identity vault (per-field metadata over the applicant profile).
identity/vault.json
identity/backups/
